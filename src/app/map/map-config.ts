import { LatLngExpression } from 'leaflet';

export const DEFAULT_MIN_ZOOM       = 0;
export const DEFAULT_MAX_ZOOM       = 18;
export const DEFAULT_INITIAL_ZOOM   = 2;
export const DEFAULT_INITIAL_CENTER = [0, 0] as LatLngExpression;
